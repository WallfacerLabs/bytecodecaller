{
  "name": "@bytecodecaller/viem",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "pnpm typecheck && biome check .",
    "lint:fix": "biome check . --apply",
    "test": "pnpm pretest && TS_NODE_PROJECT=./tsconfig.test.json vitest run",
    "pretest": "solcjs --bin ./test/integration/contracts/*.sol -o ./test/integration/build"
  },
  "dependencies": {
    "@bytecodecaller/core": "workspace:*"
  },
  "peerDependencies": {
    "viem": "^2.9.27"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.6.1",
    "@viem/anvil": "^0.0.9",
    "solc": "^0.8.25",
    "typescript": "^5.4.2",
    "vitest": "^1.5.2"
  }
}
